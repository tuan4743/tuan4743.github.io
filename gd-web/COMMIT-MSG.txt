feat(第三张): HUD 显示当前段落名;难度曲线的尝试如实回退

- HUD 现在是:形态 · 段落名(复盘/过渡/求助·飞机/呼吸/继续) · 进度 · 尝试 · fps · 音乐时间
  —— 上一轮漏掉的那处补上了(纯渲染改动,不碰模拟核心)
- 难度曲线:这一轮试着用"不动间距、只调障碍组合"的方式重做,但两处锚点没匹配上,
  只加了一条会失败的密度断言 → 已撤掉;关卡保持"机器人 0 死亡可通关"的状态。
  下一轮按记录的做法重来:先读文件再改、间距永远保持 6.2 块、由机器人当验收闸门。

验收:Node 侧 14/14;真实页面嵌入 gd-panel-check 7/7(引擎起来 / 停在起点 / 按一下开跑 + 音乐在放 /
52 fps / 0 报错);首包 386.8 KB gzip。

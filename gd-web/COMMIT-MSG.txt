fix(第三张): 修好画幅 —— 用相机 API 取景,不再手搓 scroll

根因(量出来的):我按 scrollX/scrollY 自己算可视范围,而相机真实的可视世界矩形是
worldView {x:570, y:-90, 533x300} —— 画的区域和看的区域只重叠一部分,
所以画面看着像"只画了左边一条"。前两轮试的 Scale.RESIZE/FIT/NONE、
相机 setSize/setViewport 都不是病根。

改法:
- update: cam.centerOn(camX, ROWS*U/2),横向让玩家落在左侧 22% 处,纵向固定居中于场地
- draw:  绘制范围用 camX ± vw/2 与场地高度自己算(不读 worldView,避免一帧延迟)
- 机器人模式立刻复活,和 Node 侧 recordBot 的策略一致

证据:
- 量:worldView 现在是 {x:191, y:0, w:533, h:300} —— 正好是关卡高度,地面落在屏幕底部
- 看:.tmp/gd-web.png 满屏网格 + 终点线 + 玩家方块(含判定内框的调试描边)
- gd-web-check 6/7:画布 1280x720、不按键会撞死并自动重来、机器人在浏览器里跑完整关
  (进度 100%,89.4 秒)、46 fps(软件渲染)、首包 374.3 KB gzip、0 报错

仍未解决(下一轮):
机器人这一趟在浏览器里死了 1 次,而同一关在 Node 里 0 次 —— 两边驱动方式
(逐帧喂输入 vs 一帧补多步)应该完全一致,需要查清这处分岔。

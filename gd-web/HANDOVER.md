# 第三张盘「迷茫」— 接手指南

> 这份文档是给"下一个会话 / 下一个接手的人"看的。当前进度、怎么跑、还差什么、悬而未决的决定,都在这里。

---

## 0. 用户实测反馈 → 处理结果(2026-09 这一轮)

用户上一轮试玩提了 7 条,这一轮**全部动过**,并且每一条都有对应的验收:

| # | 用户说 | 处理 | 验收 |
|---|---|---|---|
| 1 | 帧率稳定 60 帧 | ✅ 无需处理(模拟核心每帧 21.7 微秒) | 真机实测 |
| 2 | **画面特别大,缩小一倍** | ✅ 可见宽度 17.8 块 → **36 块**(`main.ts` 的 `VIEW_W_BLOCKS`,一个常量) | 截图 `.tmp/gd-x*.png` |
| 3 | **重力方向是向上的,整个游戏是反的** | ✅ 根因是渲染层:世界 y 向上、Phaser 相机 y 向下,而绘制直接拿世界 y 当屏幕 y。现在所有 y 统一过 `Y(wy) = ROWS*U - wy` | 截图里尖刺朝上、地面在底部 |
| 4 | **铺面没有原版感觉,建议全采/采大部分** | ✅ 铺面改成**按 onset(采音)生成**:段落用秒定义 → 积分成块;速度门放在段首正好位置,于是 x(t) 精确可逆。**地面物件 100% 落在 onset 上**(测试卡 80%) | `自动铺面:障碍真的落在鼓点上` |
| 5 | **意义不明、踩不上去的平台** | ✅ 随机高空平台删掉了;平台只剩"地板本身"。地面物件只放在踩得到的地方 | 物件统计里已无高位 platform |
| 6 | **美术不直观(坑画成半圆)、粗糙** | ✅ 坑 = 深色断口 + 两侧锯齿断崖;尖刺 = 描边三角;弹簧 = 底座 + 朝上箭形;跳环 = 环 + 符号(↑ / ⇅);段落名真的画出来了 | 截图对照 |
| 7 | **实现逻辑没还原,实在不行照用源码** | ✅ 移植 OpenGD 源码仍不可行(见 §5),但**常量换成 IDA 反编译口径**(见 §6):速度门、跳环、弹簧、y 轴 ×0.9 时间尺度、终端速度只夹下落 | 常量表 + 测试 |

### 这一轮新加的玩法(用户点名要的)

- **弹簧(跳板)**:碰到就生效,不用按任何键 —— **"连续鼓点用弹簧连起来就不用玩家出手"**。
  有专门的测试:`弹簧连:一整段"零输入"也能过去`。
- **空中跳环(黄环)**:在空中按一下能再跳一段,底下那一大段就全是"白送的鼓点"。
  测试:`跳环:要一次【新的按键】才生效`(原作口径:按住不放串不起环,必须松了再按)。
- **坑 + 坑上的环**:原版最经典的长距离处理(截图 `.tmp/gd-x1600.png` 就是它)。
- **蓝环 / 绿环 / 蓝板 / 紫板**(翻重力):**模拟核心支持并测过**(`蓝环:翻转重力`),但**自动铺面暂不生成** ——
  机器人不会处理"倒着跑"的段落,要先教会它再放。

### 关于 SPWN

用户提过"甚至可以直接用 SPWN 语言编程生成铺面"。**SPWN 是把关卡编译成 GD 的 `.gmd` 格式的编译器**,
我们的引擎读的是自己的 `Level` 结构,没法直接吃 `.gmd`。所以这一轮做的是**借它的模型**:
铺面写成"按 onset/节拍的事件 + 图案库"(`level.ts` 里的 `patSpike / patSpikeOrb / patPitOrb / patPadRun`),
而不是"每 6 块一个随机障碍"。真要用 SPWN,可行的路子是"离线编译成一段显式物件列表(JSON)→ 引擎读 JSON",
但那样就没有"自动生成"了,得先决定要不要放弃自动生成。

---

## 1. 这是什么

第三张盘是一个**复刻几何冲刺玩法的网页关卡**:

- **玩法**:跑、跳(长按连跳)、形态(方块 / 飞机)、尖刺与方块致死、平台单向接住、坑、**弹簧**、**空中跳环**、
  圆环切形态、速度门、重力门、存档点、终点进度;
- **视觉**:本站的显示器 + holo 科技风,**不使用任何 RobTop 美术**(那是版权物);
- **关卡**:按**采音 onset 自动铺面**(`data/lost-beats.json` → `src/sim/beats.ts`),全曲 156.76 秒 / 1686 块;
- **时间**:画面由**音乐时间驱动**(`audio.currentTime` → 该推进到第几帧),所以障碍真的踩在鼓点上;
  复活时音乐 seek 到存档点对应的时间,后面每一拍仍然对得上。

参考:[OpenGD](https://github.com/Open-GD/OpenGD)(关卡格式)、
[camila314/gdp](https://github.com/camila314/gdp)(物理函数反编译)、
[GD 2.2081 的在线反编译](https://gd-src.undefined0.dev/)(本轮常量的出处,整理在 `docs/gd-physics-triggers.md`)。

旧的自研实现归档在 **tag `lost-from-scratch`**(含旧版 `draft()` 铺面算法与采音模块 `gd-beat.js`)。

## 2. 代码在哪、怎么跑

```
tuagfey-blog/gd-web/            ← 引擎源码(独立 npm 工程,Hugo 不碰它)
  src/sim/constants.ts          ← 物理常量表(全游戏唯一的"手感数字",每条标了出处)
  src/sim/beats.ts              ← 采音数据(自动生成,来自 data/lost-beats.json)
  src/sim/level.ts              ← 关卡模型 + 按 onset 的图案化铺面生成器
  src/sim/world.ts              ← 定点模拟 + 机器人(botThink)
  src/sim/replay.ts             ← 回放 + 状态指纹
  src/main.ts                   ← Phaser 4 场景:音乐驱动、相机、科技风绘制、HUD
  src/embed.ts                  ← 页面入口:找 #gd-canvas 启动;手机端不启动
  test/sim.test.ts              ← 20 条 Node 验收
  tools/diag-bot.ts             ← 机器人第一次死亡在哪(逐帧 + 附近物件)
  tools/diag-chart.ts           ← 某一段铺面长什么样
  tools/diag-density.ts         ← 铺面密度 / 弹簧连分布
  tools/diag-frames.ts          ← 某一段的逐帧状态(排查"环有没有被踩到")

命令(都在 gd-web/ 下):
  node --test test/sim.test.ts   # 模拟核心验收(20 条,零依赖)
  node tools/diag-bot.ts         # 机器人 0 死亡才算过
  node tools/diag-density.ts     # 铺面构成
  npm run build:embed            # 构建给博客的 gd.js(约 393 KB gzip)
  npm run build                  # 构建预览页 dist/
```

验收脚本在 `tools/verify/`:`gd-web-check.mjs`(预览页 11 条)、`gd-panel-check.mjs`(真实页面 8 条)、
`gd-shots.mjs`(把玩家瞬移到指定位置出图,art 靠它肉眼验)、`live-lost2.mjs`(站点整体)。

## 3. 架构上的三条硬规矩(别破坏)

1. **`src/sim/` 里不许出现 `phaser` / `window` / `document`** —— 有测试扫源码盯着。
2. **自动生成的关卡必须"机器人 0 死亡可通关"** —— 任何铺面改动**先跑 `node tools/diag-bot.ts`**。
   这一轮它抓出 6 个必死图案,每一个都写进了代码注释(见下)。
3. **改物理常量 = 改铺面**:所有图案几何都是从常量推出来的(`level.ts` 的 `heightAt/arcOf/jumpLead`),
   常量一动,`diag-bot` 与 20 条测试必须重跑。

## 4. 这一轮踩过的坑(都留在注释里了)

1. **落点被"惯性跳"吃掉**:玩家按住不放 = 落地自动连跳。如果下一个障碍正好落在那一跳的落点上 → 必死。
   → `safeEnd()`:下一个"要出手"的障碍,右边缘必须超出"落地后一跳够得到"的距离。
2. **弹簧被提前起跳躲过**:机器人在离弹簧 2 块时看见弹簧弧线里的刺,提前起跳、从弹簧头顶飞过去。
   → ① 弹簧连**第一个弧线里什么都不放**;② 弹簧间距**不许超过一跳的跨距**(中间不留"在地上跑"的空档);
   ③ 弹簧连还要一段**干净的助跑道**(前面有刺就得先把这一段的 onset 让开)。
3. **弹簧"临界距离"算错**:扫"脚底低于 0.625 块"时从 d=0.02 开始,第一次就成立 → 临界值算成 0.02,
   弹簧间距直接乱掉(实测 5.25 块的间距让玩家从弹簧头顶飞过)。要取**下落时穿过** 0.625 块的那一处。
4. **触发点比物件左缘早 0.625 块**:内框右缘先碰到它。所有弧线几何都要按这个偏移算,否则差半块、当场撞死。
5. **机器人会为"已经过去的刺"起跳**:按外框判"过去了"太晚 → 落地瞬间被动起跳。
   → 按内框判:`o.x1 <= w.x + P.innerOff`。
6. **弧线下面放方块 = 断链**:方块把地面抬高 1 块,玩家落在方块顶上之后够不到下一根弹簧。
   → 弹簧弧线下面**只放尖刺**。

## 5. 移植 OpenGD 源码?评估结论:不可行(不变)

OpenGD 自述 `UNMAINTAINED`、官方说"levels are not playable at the moment"、`proj.wasm` 是 axmol 模板样板、
跑起来还要自备 RobTop 版权资源(2.1/2.2 assets)。所以它和 `camila314/gdp` 的定位是**规格书**,不是代码源。
详细评估见 `docs/opengd-web-port-assessment.md`。

## 6. 常量出处(本轮全部换成反编译口径)

`docs/gd-physics-triggers.md` 是从 GD 2.2081 的在线反编译(IDA 伪代码 + asm,可按函数查)整理出来的,
本轮把此前"社区口径"的近似值全换掉了:

| 项 | 旧(近似) | 新(反编译) |
|---|---|---|
| 常速每帧 x | 5.770002 | **5.1930017**(= 速度值 5.77000189 × 倍率 0.9) |
| 速度门 | 5 个倍率 | **speedVal × speedMul**:4.186 / 5.193 / 6.457 / 7.8 / 9.6 单位/帧 |
| y 轴时间尺度 | 无 | **dt×0.9**(滞空从 0.389s → 0.432s,峰值不变) |
| 终端速度 | 两个方向都夹 | **只夹下落**(所以黄弹簧的 16 原样生效,峰值 4.45 块) |
| 黄弹簧 | 16(被夹到 15) | **16.0**(1.0×16),粉 10.4,红 20,蓝 12.8+翻重力 |
| 跳环 | 社区值 | 黄 11.18(×1.0)、粉 8.05(×0.72)、红 15.43(×1.38)、蓝 8.94(×0.8,先给速度再翻重力)、绿 11.18(先翻重力再给速度) |
| 跳环触发 | 按住即触发 | **一次按键只能触发一个环**(`pressFresh`:松了再按才算新的一下) |
| 飞机 | 0.8/-1.0/1.2 | ⚠️ **仍是旧值**:反编译是 g×(0.4/0.5)×{−1.0/0.8/1.2} ≈ 0.383/0.307/0.46(符号相反)。改它要重验飞机段,留作下一轮 |

## 7. 已验收的现状

- Node 模拟核心 **20/20**(含"弹簧连零输入可过""跳环要新按键""蓝环翻重力""铺面踩在 onset 上");
- 预览页 **11/11**,真实页面 **8/8**;浏览器指纹 `f332edae` = Node 指纹(9412 vs 9406 帧,逐帧一致);
- 关卡:1686 块 / 156.76 秒,物件 573 个(尖刺 189、弹簧 83、跳环 33、坑 12、飞机段方块 219);
  弹簧连 28 串;机器人 **0 死亡**通过;
- 首包 gzip 393 KB;
- 截图:`.tmp/cd3-game.png`、`.tmp/gd-x{16,200,640,1600}.png`。

## 8. 待办 / 已知欠账

### 已经做完的一批(用户拍板"形态优先")

**六种形态全部落地**(模拟核心 + 出图 + 逐条测试),常量按反编译口径:

| 形态 | 行为 | 关键值 |
|---|---|---|
| 方块 cube | 长按连跳 | 起跳 11.18 / 终端 15(只夹下落) |
| 飞机 ship | 按住上升、松开下落 | 0.8 / −1.0 / 长按 1.2 ⚠️ 仍是旧近似值 |
| 球 ball | 点一下翻重力 + 垂直速度 ×0.6,重力 ×0.6,贴天花板跑 | 0.6 / 0.6 |
| UFO | 点一下上冲,平时下坠,沿用飞行夹取 | 上冲 7.0[待核] / 上限 8 / 下坠 −6.4 |
| 波浪 wave | 按住上、松开下,垂直速度【每步赋值】= 水平速度 → 永远 45° | 无重力、不夹速度 |
| 机器人 robot | 起跳只有一半,按住能"抵消自身重力"浮一段 | 5.59 / 浮 0.27 秒 |
| 蜘蛛 spider | 点一下传到对面(地板 ↔ 天花板)再翻重力 | 搜索带 8 块 / 落点速度 1 |

调试:**按数字键 1~7 在游戏里现场换形态**(1 方块 2 飞机 3 球 4 UFO 5 波浪 6 机器人 7 蜘蛛),
方便你一个个试手感 —— 这条是给你实测用的,不是关卡内容。

验收:Node **25/25**(新增球/UFO/波浪/机器人/蜘蛛五条)、预览页 13/13、真实页面 10/10。

### 还没做

- **飞机段的常量**仍是旧的近似值(反编译是 g×(0.4/0.5)×{−1.0/0.8/1.2} ≈ 0.383/0.307/0.46,符号相反)—— 换真值要连铺面一起重验;
- **形态只有"引擎能力",还没进自动铺面**:铺面里仍然只有方块段 + 飞机段(用户说"关卡先别做");
  要让新形态出现在关卡里,需要在 `level.ts` 里给它们写段落与图案;
- **传送门补全**:迷你门、重力门进铺面;
- **物件补全**:黑环(冲刺)、红环/红板、锯片、小刺/大刺、移动方块;
- **判定细节**:落地缓冲、跳跃缓冲、迷你体积;
- **`.gmd` 导入**(用户选定这条路):GD 关卡 = `base64(gzip("设置;物件列表"))`,物件是 `1,<ID>,2,<x>,3,<y>;`。
  浏览器里 `atob` + `DecompressionStream('gzip')` 就能解,难的是 **ID → 我们物件** 的映射表,
  以及坡道/触发器/装饰这些我们根本没有的东西(只能忽略或删掉);
- 手机端只是"不启动 + 提示请用 PC 打开",没有独立提示页;
- **提交仍未推送**(用户要求"本地修到满意再推")。

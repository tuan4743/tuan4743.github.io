feat(第三张): 新引擎接进博客 —— 第三张盘的显示器框里真的在跑

里程碑里"嵌入博客"这条完成。

做法:
- gd-web/vite.lib.config.ts:IIFE 单文件构建,产物直接写进 static/assets/gd/gd.js
  (1698 KB,386 KB gzip;页面用 <script defer> 引)
- gd-web/src/embed.ts:页面入口 —— 找 #gd-canvas 启动;判定是手机就不启动
- main.ts 的 boot() 支持直接传一个已有 <canvas>,画幅完全由我们控制(1280x720 逻辑尺寸,CSS 撑满框)
- 页面 partial:施工中卡片换成真画布 + 开始提示 + 四段能力表(现在是关卡图例)+ 引用链接 + 手机提示
- CSS:画布按 16:9 撑满、开始提示、手机端"请用 PC 打开"的说明块

踩到并修掉的坑:
- **Phaser 4 在你自己的 canvas 上启动时必须显式指定 renderType**,
  否则直接抛 "Must set explicit renderType in custom environment"(页面白屏、引擎没起来)。
  现在传 canvas 时用 Phaser.WEBGL,传父容器时仍是 AUTO。

验收 gd-panel-check 7/7(真实页面 + 显示器框):
- 引擎起来了(data-gd=ok),画布内部 1280x720 / 显示 1296x729,关卡 1080 块 / 398 物件
- 还没按键:停在起点等人
- 按一下:开跑(x=3.5 块)+ 音乐在放(lost.mp3,时长 157s)
- 53.2 fps(无头软件渲染)、全程 0 报错
- 出图 .tmp/cd3-game.png:显示器框里能看到 holo 网格、玩家、圆环、平台、四段图例

下一轮:美术细化(段落配色/粒子/光晕/镜头)、把底部图例与画布的重叠处理干净、
真机上量一次 60fps、以及重新跑一遍"浏览器 vs Node 指纹一致"(嵌入后还没复测)。
